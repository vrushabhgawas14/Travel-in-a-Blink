export const metadata = {
  title: "Reviews",
  description: "Read user reviews and share your own.",
};

export default function ReviewLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
